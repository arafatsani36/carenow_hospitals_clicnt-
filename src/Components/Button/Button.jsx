import React from 'react';

const Button = (props) => {
    
        <Button className=" text-black font-bold text-8xl">
            {props.children}
        </Button>
       
   
};

export default Button;